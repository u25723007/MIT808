# MIT808

The EDA file is just a single file code that can be run either on google colab (Prefered) or local machine notebook.

Before you run the file make sure the data path location matches your, otherwise update the path accorddingly.

  Look for this line:
  
  FILE_PATH = "/content/drive/MyDrive/Capstone/Copy of Copy of Copyright and African NLP [Database] (Responses).xlsx"
  
  SHEET_NAME = "Updated database"
  
if you are running from local machine update the path string

To ru the the file just press run all cells button, fron top to allow library importation
